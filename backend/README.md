# Fahad Live Backend

Minimal FastAPI backend for Fahad Live voice avatar.

## Running

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Environment
Copy `.env.example` to `.env` and fill the required keys.
